# Railway Configuration for FakeCarrier Monorepo

[build]
builder = "NIXPACKS"
buildCommand = "echo 'Building both services...'"

[deploy]
startCommand = "echo 'Use separate services for API and Web'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
